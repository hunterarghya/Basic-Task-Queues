# Basic-Task-Queues

Experimenting how task queues and background task scheduling are implemented using **celery worker**, **celery beat**, **redis** and **rabbitmq**

---

## 🐳 Run with Docker

### Clone the repository

```bash
git clone <repo-url>
cd Basic-Task-Queues
```

### Build & Run with Docker Compose

```bash
docker compose up --build
```
